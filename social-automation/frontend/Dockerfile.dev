FROM node:20-alpine

WORKDIR /app

RUN apk update && apk upgrade --available

# Install deps from package.json only — source is volume-mounted at runtime
COPY package*.json ./
RUN npm ci --legacy-peer-deps

EXPOSE 8083

CMD ["npx", "next", "dev", "--hostname", "0.0.0.0", "--port", "8083"]
