{
  "mcpServers": {
    "playwright": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "--init", "--network", "host", "--shm-size=2g", "-v", "/home/tbaltzakis/cu130-slim:/workspace", "-w", "/workspace", "mcr.microsoft.com/playwright/mcp:latest", "--headless"]
    },
    "github-local": {
      "command": "/home/tbaltzakis/.local/share/pnpm/mcp-server-github",
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
