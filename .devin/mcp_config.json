{
  "mcpServers": {
    "playwright": {
      "command": "docker",
      "args": ["run", "--rm", "-i", "--init", "--network", "host", "-v", "/home/tbaltzakis/cu130-slim:/workspace", "-w", "/workspace", "mcr.microsoft.com/playwright/mcp:v0.0.79", "--headless"],
      "disabled": true
    },
    "github-local": {
      "command": "/home/tbaltzakis/.local/share/pnpm/mcp-server-github",
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${env:GITHUB_PERSONAL_ACCESS_TOKEN}"
      }
    }
  }
}
