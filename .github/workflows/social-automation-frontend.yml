name: Social Automation Frontend CI

on:
  push:
    branches: [ master ]
    paths:
      - 'social-automation/frontend/**'
  pull_request:
    branches: [ master ]
    paths:
      - 'social-automation/frontend/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Cache node modules
        uses: actions/cache@v4
        with:
          path: social-automation/frontend/node_modules
          key: ${{ runner.os }}-node-${{ hashFiles('social-automation/frontend/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Install dependencies
        run: |
          cd social-automation/frontend
          npm ci

      - name: Run linting
        run: |
          cd social-automation/frontend
          npm run lint

      - name: Run type checking
        run: |
          cd social-automation/frontend
          npm run typecheck

      - name: Run tests
        run: |
          cd social-automation/frontend
          npm run test -- --run

      - name: Build
        run: |
          cd social-automation/frontend
          npm run build
